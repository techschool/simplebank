apiVersion: v1
kind: ConfigMap
metadata:
  name: aws-auth
  namespace: kube-system
data:
  mapUsers: |
    - userarn: arn:aws:iam::<AWS_ACCOUNT_ID>:user/<CI_USERNAME>
      username: <CI_USERNAME>
      groups:
        - simple-bank-deployer  # Scoped role — do NOT use system:masters
